[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["backend/tests", "k8s/tests"]
addopts = "-v --tb=short"
markers = [
  "e2e: end-to-end tests (may require external services like Docker/Redis/Postgres)",
]
