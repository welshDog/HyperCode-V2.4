# Project Context

The canonical project context lives at the repo root:

**`H:\HyperStation zone\HyperCode\HyperCode-V2.4\CLAUDE_CONTEXT.md`** — read that first.

Also at root: `CLAUDE.md`, `CLAUDE_START.md`.

This `.claude/` folder holds local Claude config (skills, settings) only.
