# MCP Python SDK — PINNED EXACT on purpose. Two separate traps here:
#   1. v2 (stable 2026-07-28) is a protocol rewrite: `mcp.server.fastmcp.FastMCP`
#      / `mcp.sse_app()` (used in server.py) don't exist there -> Exits(1) on
#      startup (the Sept 2026 prod-outage failure class).
#   2. Even within v1.x, minor bumps change behaviour: >=1.9 turned on
#      DNS-rebinding protection by default (Host allow-list), which 421'd every
#      request by Docker service name until server.py set transport_security.
# server.py is validated against exactly 1.27.1 (custom_route + the
# TransportSecuritySettings kwarg). Bump deliberately + re-test, never with `>=`.
mcp[cli]==2.2.0
httpx>=0.28.0,<1
uvicorn>=0.35.0,<1
