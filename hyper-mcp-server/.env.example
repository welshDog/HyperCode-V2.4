# Hyper MCP Server — copy to .env and fill in values
# NEVER commit the real .env to git!

BROSKI_AGENT_URL=http://broski-agent:8001
BRAIN_CORE_URL=http://brain-core:8002
# Point this at the live HYPER-SILLs vault (Railway) so resources/read serves real skill bodies.
SKILLS_API_URL=https://hyper-sills-by-welshdog-production.up.railway.app

# Optional: when set, every POST /mcp must send  X-API-Key: <this value>
# Leave blank for local dev (open). MUST be set on the public Railway deploy.
MCP_API_KEY=
